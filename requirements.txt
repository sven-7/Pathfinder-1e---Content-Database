# Pathfinder 1e Content Database - Python dependencies
# Install: pip install -r requirements.txt

# Core
requests>=2.28.0
beautifulsoup4>=4.11.0

# HTML parsing (faster parser for BeautifulSoup)
lxml>=4.9.0

# Already likely installed
# sqlite3 (stdlib)
# json (stdlib)
# pathlib (stdlib)

# Phase 3: Web API + Character Creator
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
jinja2>=3.1.0

# Phase 11: Multi-User Backend
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29.0
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0
alembic>=1.13.0
python-dotenv>=1.0.0

# Phase 12b: Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
